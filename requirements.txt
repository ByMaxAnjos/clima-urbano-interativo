streamlit>=1.49.1
pandas>=2.0.0
numpy>=1.26.0
geopandas>=1.0.0
folium>=0.20.0
streamlit-folium>=0.25.0
plotly>=5.0.0
shapely>=2.0.0
branca>=0.8.0
streamlit-option-menu>=0.3.12
urllib3
# Dependências para LCZ4r/LCZ4py
rasterio
requests
xarray
h5netcdf
pystac-client
planetary-computer
# xarray/h5netcdf e pystac-client/planetary-computer são usados por
# lcz_get_lst (temperatura de superfície) e lcz_grid_pollution_ghap
# (qualidade do ar), ambos em Explorar, mas não estão nas dependências core
# do LCZ4py apesar de serem necessários em tempo de execução: xarray com
# engine="h5netcdf" para ler os arquivos do Sentinel-3, e pystac-client +
# planetary-computer para localizar as cenas Sentinel-3 no catálogo STAC do
# Planetary Computer (import lazy, só falha quando source="sentinel3" é
# realmente usado — por isso passou despercebido em teste local, onde os
# dois já estavam instalados por outro motivo).
#
# LCZ4py não está publicado no PyPI ainda — instalado direto do GitHub
# (traz como dependências: polars, pyarrow, scipy, httpx, aiohttp, rioxarray,
# tenacity, diskcache, kaleido, nest_asyncio, matplotlib, entre outras — ver
# o pyproject.toml do LCZ4py). Fixamos um commit específico para não quebrar
# o deploy silenciosamente se o pacote mudar; atualize o hash ao testar uma
# nova versão.
LCZ4py @ git+https://github.com/ByMaxAnjos/LCZ4py.git@f9b08d9
